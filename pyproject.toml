[project]
name = "py-typing-1-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "pyqt6>=6.7.1,<6.8; sys_platform == 'linux' and platform_machine == 'aarch64'",
    "pyqt6>=6.11.0; sys_platform != 'linux' or platform_machine != 'aarch64'",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
